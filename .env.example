# Source: SEONGON's Supabase (read-only). Used by src/atlas/pull.py
# to extract raw SERP / AIO data into local parquet.
SUPABASE_PG_HOST=aws-1-ap-southeast-2.pooler.supabase.com
SUPABASE_PG_PORT=5432
SUPABASE_PG_DATABASE=postgres
SUPABASE_PG_USER=postgres.jjhpvvtcblnsyfwnmxho
SUPABASE_PG_PASSWORD=

# Atlas's own analytical Postgres (Railway). Stores the cleaned and
# derived data: keyword_results, exploded citations + top-10, findings
# tables, eventually dashboard read-views. Connection string from
# `railway variables --kv` (look for DATABASE_PUBLIC_URL).
ATLAS_PG_URL=
